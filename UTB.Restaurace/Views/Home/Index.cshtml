<div class="container text-center">
    <div class="row g-4 justify-content-center">
        <!-- První řada (2 čtverce) -->
        <div class="col-12 col-md-6 col-lg-3">
            <div class="square" onclick="location.href='@Url.Action("Index", "Menu")'">
                <img id="theme-image" src="~/img/icons/MealMenu.png" alt="Meals Menu" class="img-fluid theme-adaptive-image" />
                <div class="overlay">Meals Menu</div>
            </div>
        </div>
        <div class="col-12 col-md-6 col-lg-3">
            <div class="square" onclick="location.href='@Url.Action("Index", "Drinks")'">
                <img id="theme-image" src="~/img/icons/DrinksMenu.png" alt="Drinks Menu" class="img-fluid theme-adaptive-image" />
                <div class="overlay">Drinks Menu</div>
            </div>
        </div>
    </div>

    <div class="row g-4 justify-content-center mt-3">
        <!-- Druhá řada (2 čtverce) -->
        <div class="col-12 col-md-6 col-lg-3">
                <div class="square" onclick="location.href='@Url.Action("Create", "UserReservation", new { area = "User" })'">
                <img id="theme-image" src="~/img/icons/Reservation.png" alt="Reservation" class="img-fluid theme-adaptive-image" />
                <div class="overlay">Reservation</div>
            </div>
        </div>
        <div class="col-12 col-md-6 col-lg-3">
            <div class="square">
                <div class="info-box">
                    <h4>Phone Number: +420 123 456 789</h4>
                    <h4>Address: Zlín 1, Zlín</h4>
                    <h4>Opening hours: Every day 10:00-22:00</h4>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    body {
        margin: 0; /* Odstraní výchozí margin */
        height: 100vh; /* Zajistí, že tělo pokryje celou výšku okna */
        position: relative; /* Nastavíme pozici pro pseudo-element */
        overflow: hidden; /* Zabráníme přetečení */
    }

        body::before {
            content: ""; /* Musíme přidat obsah, aby pseudo-element fungoval */
            position: absolute; /* Umožní nám umístit ho přesně */
            top: 0; /* Umístíme ho na začátek */
            left: 0; /* Umístíme ho na začátek */
            right: 0; /* Rozšíříme ho na šířku */
            bottom: 0; /* Rozšíříme ho na výšku */
        }

        /* Light Theme (Default) */
        body.light-theme {
            background-color: #ffffff;
            color: #000000;
        }

        /* Dark Theme */
        body.dark-theme {
            background-color: #1a1a1a;
            color: #ffffff;
        }

    .square {
        width: 100%; /* Zajistí, že čtverce budou mít 100% šířky sloupce */
        height: 300px;
        border: 5px solid black;
        position: relative;
        cursor: pointer;
    }

        .square img {
            background-color: white;
            width: 100%;
            height: 100%;
            display: block;
        }

    .overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: rgba(255, 255, 255, 0.8);
        color: black;
        font-size: 20px;
        opacity: 0;
        transition: opacity 0.3s;
    }

    .square:hover .overlay {
        opacity: 1;
    }

    .info-box {
        background-color: lightgray;
        height: 100%;
        padding: 10px;
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
    }

    .theme-adaptive-image {
        transition: filter 0.3s ease;
    }

    body.light-theme .theme-adaptive-image {
        filter: invert(0%);
    }

    body.dark-theme .theme-adaptive-image {
        filter: invert(100%);
    }

</style>
